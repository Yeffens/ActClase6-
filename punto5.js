let quantityOfMeters = prompt("Ingrese la cantidad de metros cuadrados a pintar")
let princeByMeter = 10000
let totalprice = 0

totalprice = parseFloat(quantityOfMeters) * princeByMeter

alert ("El precio total a pgar es:" + totalprice)


